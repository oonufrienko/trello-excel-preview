.env
.env.local
.vercel
node_modules
trello-guide
trello-terms

# OS / IDE
.DS_Store
.claude/

# Test artefacts (per-run, not source)
storageState.json
test-results/
playwright-report/
tests/fixtures/generated/
tests/fixtures/real/
tests/fixtures/.attachment-ids.json

# Local fixtures with real data (never commit)
bad_excel/

# Marketplace screenshots (uploaded directly to Atlassian, not via git)
marketing/screenshots/
.env*

# Generated knowledge graph (rebuild locally via /graphify)
graphify-out/cache/
graphify-out/cost.json
# machine-specific: absolute local paths, regenerated by graphify
graphify-out/.graphify_python
graphify-out/.graphify_root
# dated backup folders graphify update creates before rewriting
graphify-out/20*/
